use crate::models::game::Game;

use super::game_modes::GameMode;

pub fn run_abondance(game: Game, game_mode: GameMode) -> Game {
    game
}
pub fn run_solo_slim(game: Game, game_mode: GameMode) -> Game {
    game
}
